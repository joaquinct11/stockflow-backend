# ========================================
# CONFIGURACIÓN BASE - STOCKFLOW
# ========================================

spring:
  application:
    name: StockFlow

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jackson:
    time-zone: America/Lima
    date-format: yyyy-MM-dd'T'HH:mm:ss

# Servidor
server:
  port: 8080
  servlet:
    context-path: /api

# JWT
jwt:
  secret: ${JWT_SECRET}
  expiration: 900000           # 15 minutos (900000)
  refresh:
    expiration: 604800000    # 7 días

# ? AGREGAR ESTO - CORS por defecto
cors:
  allowed-origins:
    - http://localhost:5173
    - http://localhost:3000
    - http://127.0.0.1:5173

# Logging
logging:
  level:
    root: INFO
    com.stockflow: DEBUG
    org.springframework.security: DEBUG

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

mercadopago:
  access-token: ${MERCADOPAGO_ACCESS_TOKEN:TEST-2763402378969817-042315-e2056124374581960671ee04e8465e20-293036685}
  webhook-secret: ${MERCADOPAGO_WEBHOOK_SECRET:b222f7d75228c234f3f4f0ab7669f2e25aef4d9e8ff39deb856038c0672beece}
  currency-id: ${MERCADOPAGO_CURRENCY_ID:PEN}
  checkout-base-url: ${MERCADOPAGO_BASE_URL:https://api.mercadopago.com}
  notification-url: ${MERCADOPAGO_NOTIFICATION_URL:https://dislike-curing-sprout.ngrok-free.dev/api/webhooks/mercadopago?token=b222f7d75228c234f3f4f0ab7669f2e25aef4d9e8ff39deb856038c0672beece}
  success-url: ${MERCADOPAGO_SUCCESS_URL:https://stockflow-frontend-uat.vercel.app/checkout/success}
  failure-url: ${MERCADOPAGO_FAILURE_URL:https://stockflow-frontend-uat.vercel.app/checkout/failure}
  pending-url: ${MERCADOPAGO_PENDING_URL:https://stockflow-frontend-uat.vercel.app/checkout/pending}
